-- A program to insert blank pages every other page. For simplicity, a blank
-- page is also inserted at the end.

source = read('source.pdf');

result = [];
for page in source {
    result += [page, blankPage()];
}

write('result.pdf', result);
